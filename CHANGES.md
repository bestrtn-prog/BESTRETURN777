# breaksPL101318.mq4 変更履歴

## バージョン 1.06 - 起動時過去分析機能追加

### 追加機能

1. **起動時過去200本のバー分析**
   - EA起動時に過去200本（デフォルト、パラメータで変更可能）の足を分析
   - 取引ルールに基づいて仮想的に過去のエントリー・エグジットを再現
   - 現在の状態（ポジション中 or 待機中）を自動判定

2. **状態復元機能**
   - 過去の分析結果から現在の状態を復元
   - 最後のトレードが勝ちか負けかを判定
   - 仮想ポジション中の場合はポジション情報を復元
   - 実ポジションが存在する場合は検出

3. **起動時ポジション処理**
   - EA起動時に既存の実ポジションが存在する場合、次のティックで自動クローズ
   - クローズ後は次のエントリーシグナルまで待機
   - 仮想ポジション中の場合は状態を維持

### 追加パラメータ

- `HistoryBars` (int, デフォルト: 200)
  - 起動時に分析する過去のバー数

### 追加関数

1. `AnalyzeHistoricalBars()`
   - 過去のバーを分析して現在の状態を判定
   - エントリー・エグジット条件を過去に遡って適用
   - `LastTradeWasLoss`フラグと仮想ポジション状態を復元

2. `CloseAllPositions()`
   - EA起動時に既存の実ポジションをすべてクローズ
   - 最大3回までリトライ

### 使用シナリオ

#### シナリオ1: 待機中に起動
- 過去の分析結果から最後のトレードが勝ちか負けかを判定
- 次のエントリーシグナルまで待機

#### シナリオ2: 仮想ポジション中に起動
- 仮想ポジションを復元（現在価格でエントリーしたものとして扱う）
- 次のティックからエグジット条件をチェック開始

#### シナリオ3: 実ポジション保有中に起動
- 既存の実ポジションを検出
- 次のティックで自動クローズ
- クローズ後は次のエントリーまで待機

### 注意事項

- 過去の分析は簡略化されており、スプレッドチェックなどは省略されています
- 仮想ポジションの復元時、ロット数は0に設定されます（実際の取引には影響しません）
- 実ポジションのクローズは起動後の最初のティックで実行されます
